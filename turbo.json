{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["generate", "^build", "^generate"],
      "outputs": ["dist/**", ".next/**"]
    },
    "generate": {
      "cache": false
    },
    "dev": {
      "dependsOn": ["^build"],
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["generate", "^build"]
    },
    "typecheck": {
      "dependsOn": ["generate", "^build"]
    },
    "test": {
      "dependsOn": ["generate", "^build"]
    }
  }
}
